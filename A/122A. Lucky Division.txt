#include <bits/stdc++.h>
using namespace std;
//const ll M = 1e9+7;
//ll gcd(ll a, ll b) { return !b ? a : gcd(b , a % b); }
//ll lcm(ll x, ll y) { return (x * y / gcd(x, y)); }
//ll fact(ll z)      { return (z <= 1) ? 1 : z * fact(z - 1); }
//len any int num > 0 ... len = ceil(log10(a+1));
int  n , m , o , A []= {4 , 47 , 74 , 447 , 474 , 7 , 747 , 774  , 477 , 744};
bool f;
int main(){
    scanf("%d" , &n);
   for(int i = 0; i< 10; ++i)
    if( n % A[i] == 0){
        puts("YES");
        return 0;
   }
   puts("NO");
    return 0;
}
