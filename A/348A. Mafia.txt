#include <bits/stdc++.h>
#define ll long long
#define eps 1e-15
using namespace std;
const int B = 15 , N = 1e6+6 , MAX = 2e5+5  , OO = 1000000009;
ll gcd(ll a, ll b) { return !b ? a : gcd(b , a % b); }
ll lcm(ll x, ll y) { return (x * y / gcd(x, y)); }
ll fact(ll z)      { return (z <= 1) ? 1 : z * fact(z - 1); }
//len any int num > 0 ... len = ceil(log10(a+1));
ll n , A[N] , maxii = INT_MIN , ans , m = INT_MAX , sum;
int main(){
    scanf("%lld" , &n);
    for(int i = 0 ; i < n ; ++i)    scanf("%lld" , &A[i]) ,sum += A[i] ,  maxii = max(maxii , A[i]);
    ll s = 1 , e = 1e12+12 ;
    while(s <= e){
        ll med = s +((e - s) >> 1ll);
        if(med >= maxii && n * med - med >= sum)  m = med  ,  e = med - 1;
        else                                      s = med + 1;
    }
    printf("%lld\n" , m);
    return 0;
}
