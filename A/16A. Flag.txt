#include <bits/stdc++.h>
#define ll long long
using namespace std;
//const ll M = 1e9+7;
//ll gcd(ll a, ll b) { return !b ? a : gcd(b , a % b); }
//ll lcm(ll x, ll y) { return (x * y / gcd(x, y)); }
//ll fact(ll z)      { return (z <= 1) ? 1 : z * fact(z - 1); }
//len any int num > 0 ... len = ceil(log10(a+1));
int  n , m;
set<char>st;
bool f ;
string s , ss;
int main(){
     scanf("%d %d" , &n , &m);
        for(int i = 0; i < n ; ++i){
                cin >> s;
                for(int j = 0; j < m ; ++j)
                    st.insert(s[j]);
                    if(st.size() > 1) f = 1;
                else {
                    ss += *st.begin();
                    st.clear();
                }
        }
            if(f) return puts("NO") , 0;
             for(int i = 0; i < n ; i+=2) if(ss[i] == ss[i+1]) return puts("NO") , 0;
                 puts("YES");
                 return 0;
}


